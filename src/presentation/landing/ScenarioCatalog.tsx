import { CheckCircle2, LockKeyhole, Network } from "lucide-react";
import { scenarioCatalog } from "@/domain/catalog/scenarioCatalog";
import { Panel } from "../shared/ui";

export function ScenarioCatalog({ onSelectScenario, onSelectBlackbox }: { onSelectScenario: (scenarioId: string) => void; onSelectBlackbox: () => void }) {
  return <section className="border-t border-white/[0.08] pt-12"><div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan-300/70">Learning path</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-100">Ten incidents. One reliable instinct.</h2></div><p className="max-w-sm text-right text-xs leading-5 text-slate-500">Start with clear signals, then learn to reason across systems and uncertainty.</p></div><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-5">{scenarioCatalog.map((entry) => { const available = entry.status === "available"; return <button key={entry.id} type="button" onClick={() => available && onSelectScenario(entry.id)} disabled={!available} aria-label={available ? `Select ${entry.title}` : `${entry.title} coming later`} className="text-left disabled:cursor-not-allowed"><Panel as="div" className={`h-full p-4 transition ${available ? "border-cyan-300/25 bg-cyan-300/[0.04] hover:border-cyan-200/50" : "opacity-75"}`}><div className="flex items-start justify-between gap-2"><span className="font-mono text-xs text-slate-600">{String(entry.order).padStart(2, "0")}</span>{available ? <CheckCircle2 size={15} className="text-emerald-300" /> : <LockKeyhole size={14} className="text-slate-600" />}</div><h3 className="mt-4 min-h-10 text-sm font-semibold leading-5 text-slate-200">{entry.title}</h3><div className="mt-3 flex items-center justify-between gap-2 text-[10px] uppercase tracking-[0.13em] text-slate-500"><span>{entry.difficulty}</span><span>{entry.estimatedMinutes} min</span></div><p className="mt-3 text-xs leading-5 text-slate-500">{available ? "Playable now" : "Coming later"}</p></Panel></button>; })}</div><div className="mt-14 border-t border-white/[0.08] pt-10"><div className="mb-6 flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan-300/70">Practice modes</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-slate-100">Try a different way to investigate.</h2></div><p className="max-w-sm text-right text-xs leading-5 text-slate-500">Explore a local system through signals, interfaces, and consequences.</p></div><button type="button" onClick={onSelectBlackbox} aria-label="Enter Checkout Blackbox" className="w-full max-w-md text-left"><Panel as="div" className="p-5 transition border-cyan-300/25 bg-cyan-300/[0.04] hover:border-cyan-200/50"><div className="flex items-start justify-between gap-3"><div className="rounded-xl border border-cyan-300/20 bg-cyan-300/10 p-3 text-cyan-200"><Network size={20} /></div><span className="rounded-full border border-emerald-300/20 bg-emerald-300/10 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.14em] text-emerald-200">Playable now</span></div><h3 className="mt-5 text-lg font-semibold text-slate-100">Checkout Blackbox</h3><p className="mt-2 text-sm leading-6 text-slate-400">Explore a degraded checkout system through observable signals.</p><div className="mt-4 flex items-center justify-between text-[10px] uppercase tracking-[0.13em] text-slate-500"><span>Beginner</span><span>8 min</span></div></Panel></button></div></section>;
}
