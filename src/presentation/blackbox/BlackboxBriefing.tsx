import { ArrowRight, ArrowLeft, Eye, FlaskConical, ShieldCheck } from "lucide-react";
import { Panel, PrimaryButton, SecondaryButton } from "../shared/ui";

export function BlackboxBriefing({ hasSavedSession, onStart, onResume, onBack }: { hasSavedSession: boolean; onStart: () => void; onResume: () => void; onBack: () => void }) {
  return <main className="min-h-screen bg-[#080b10] px-5 py-8 text-slate-100 sm:px-8 lg:px-12"><div className="mx-auto max-w-4xl"><header className="flex items-center justify-between border-b border-white/[0.08] pb-6"><span className="text-sm font-bold tracking-[0.16em]">FAILURE LAB</span><span className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan-200/70">Practice mode</span></header><div className="pt-16"><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-cyan-200/80">Local deterministic exercise</p><h1 className="mt-3 text-5xl font-semibold tracking-tight text-white">Checkout Blackbox</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-400">Investigate a degraded checkout system through signals, interfaces, and consequences. The target will not explain itself: form a hypothesis, run an experiment, and watch what changes.</p><div className="mt-10 grid gap-4 sm:grid-cols-3"><Panel className="p-5"><Eye className="text-cyan-200" size={20} /><h2 className="mt-4 font-semibold">Observable evidence</h2><p className="mt-2 text-sm leading-6 text-slate-500">See only dashboards, endpoints, service signals, and side effects.</p></Panel><Panel className="p-5"><FlaskConical className="text-amber-200" size={20} /><h2 className="mt-4 font-semibold">Run experiments</h2><p className="mt-2 text-sm leading-6 text-slate-500">Spend limited actions to test your operational hypothesis.</p></Panel><Panel className="p-5"><ShieldCheck className="text-emerald-200" size={20} /><h2 className="mt-4 font-semibold">Stabilize locally</h2><p className="mt-2 text-sm leading-6 text-slate-500">This is a safe local simulation, not a real infrastructure target.</p></Panel></div><div className="mt-10 flex flex-wrap gap-3">{hasSavedSession && <PrimaryButton onClick={onResume} className="gap-2">Resume Blackbox <ArrowRight size={16} /></PrimaryButton>}<PrimaryButton onClick={onStart} className="gap-2">Start new Blackbox <ArrowRight size={16} /></PrimaryButton><SecondaryButton onClick={onBack} className="gap-2"><ArrowLeft size={16} />Back to catalog</SecondaryButton></div></div></div></main>;
}
