import { Check, ChevronRight, Clock3, LockKeyhole, Zap } from "lucide-react";
import type { IncidentAction } from "@/domain/incident/types";

export function ActionCard({ action, available, completed, disabledReason, onSelect }: { action: IncidentAction; available: boolean; completed: boolean; disabledReason?: string; onSelect: () => void }) { return <button type="button" onClick={onSelect} disabled={!available || completed} className={`group w-full rounded-xl border p-4 text-left transition ${completed ? "border-emerald-300/15 bg-emerald-300/[0.04]" : available ? "border-white/[0.08] bg-white/[0.025] hover:border-cyan-300/40 hover:bg-cyan-300/[0.05]" : "cursor-not-allowed border-white/[0.05] bg-white/[0.015] opacity-50"}`}><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="text-sm font-semibold text-slate-200">{action.title}</span>{completed ? <Check size={15} className="text-emerald-300" /> : !available ? <LockKeyhole size={14} className="text-slate-500" /> : null}</div><p className="mt-1.5 text-xs leading-5 text-slate-400">{action.description}</p>{disabledReason && <p className="mt-2 text-[10px] font-semibold uppercase tracking-[0.12em] text-amber-200/80">{disabledReason}</p>}</div>{available && !completed && <ChevronRight size={17} className="mt-0.5 shrink-0 text-slate-600 transition group-hover:translate-x-0.5 group-hover:text-cyan-200" />}</div><div className="mt-3 flex items-center gap-4 text-[10px] font-semibold uppercase tracking-[0.14em] text-slate-500"><span className="inline-flex items-center gap-1"><Clock3 size={12} /> +{action.timeCostMinutes} min</span><span className="inline-flex items-center gap-1"><Zap size={12} /> {action.actionPointCost} {action.actionPointCost === 1 ? "point" : "points"}</span></div></button>; }
